/* 6. Convert a String to Uppercase
Write a function that converts all lowercase letters in a string to uppercase. */

function toUpperCase(str){
    return str.toUpperCase();
}
console.log(toUpperCase("qamrul"));